html {
    font-family: map(fonts, primary);
    height: 100%;
    min-width: 480px;
    min-height: 600px;
}
body {
    margin: 0;
    padding: 0;
    height: 100%;
    min-width: 480px;
    min-height: 600px;
    background-color: map(colors, white);
}

#root {
    height: 100%;
}

html {
    font-family: map(fonts, primary);
    height: 100%;
    min-width: 400px;
    min-height: 450px;
}
body {
    margin: 0;
    padding: 0;
    height: 100%;
    min-width: 400px;
    min-height: 450px;
    background-color: map(colors, white);
}

#root {
    height: 100%;
}
